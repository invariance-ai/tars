# @invariance/tars

The `tars` CLI — anonymous, opt-in orchestration telemetry for AI coding agents (Claude Code, Codex, Cursor).

```bash
npx @invariance/tars init
```

Full docs: https://github.com/invariance-ai/tars
